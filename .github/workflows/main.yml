name: Main Workflow

on:
  push:
    branches: [ "main" ]

jobs:
  run-composite-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run custom composite action
        uses: ./.github/actions
